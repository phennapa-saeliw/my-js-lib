// src/index.js

// ฟังก์ชันตัวอย่าง
function add(a, b) {
    return a + b;
}

// ฟังก์ชันตัวอย่าง
function subtract(a, b) {
    return a - b;
}

// ทำการ export ฟังก์ชัน
module.exports = {
    add,
    subtract
};
